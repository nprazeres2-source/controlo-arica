// Acrescente estes dois blocos dentro de "match /databases/{database}/documents { ... }"
// nas regras do Firestore (Consola Firebase > Firestore Database > Regras).
// Mantenha as regras que já tem para as outras apps (ex.: fardamento).
// Só quem fez login na app (utilizador AHBVF) consegue ler e gravar.

    match /aricas_ocorrencias/{id} {
      allow read, write: if request.auth != null;
    }
    match /aricas_config/{id} {
      allow read, write: if request.auth != null;
    }
